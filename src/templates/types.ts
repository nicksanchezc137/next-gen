export const GENERAL_TYPES = () => {
  return {
    contents: `
    export type Menu ={
        destination:string;
        label:string; 
    }
      `,
    fileName: "general.types.ts",
    path: "path",
  };
};
